#include "calculadora.h"
#include <stdio.h>

int main() {
printf("Soma: %.2f\n", soma(1.1, 2.2));
printf("Subtração: %.2f\n", subtrai(1.1, 2.2));
printf("Multiplicação: %.2f\n", multiplica(1.1, 2.2));
printf("Divisão: %.2f\n", divide(1.1, 2.2));
return 0;
}